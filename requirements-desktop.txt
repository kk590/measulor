# Requirements for Desktop/Local Development
# Use this file for running the GUI application locally with camera access
# Install with: pip install -r requirements-desktop.txt

# Core dependencies (Python 3.13 compatible)
Flask==3.0.0
opencv-python>=4.9.0
mediapipe>=0.10.9
numpy>=1.26.0

# Note: Use opencv-python (NOT opencv-python-headless) for camera support
